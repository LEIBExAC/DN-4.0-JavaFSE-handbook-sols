Note -> Listing the commands which I ran in this handson

echo "Log data" > example.log

mkdir log
echo "Log inside folder" > log/logfile.log

touch .gitignore

git add .gitignore
git commit -m "Add .gitignore to ignore .log files and log folder"
